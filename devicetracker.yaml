  - platform: fritz
    host: 192.168.178.1
    username: !secret fritzbox_username
    password: !secret fritzbox_password
  - platform: luci
    host: 192.168.100.1
    username: !secret openwrt1_username
    password: !secret openwrt1_password
  - platform: luci
    host: 192.168.100.2
    username: !secret openwrt2_username
    password: !secret openwrt2_password

#  - platform: nmap_tracker
#    hosts: 192.168.178.1/24
#    home_interval: 5
#    scan_options: -F --host-timeout 30s
#  - platform: owntracks
#    max_gps_accuracy: 200
